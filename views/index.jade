extends nav

include ./mixins.jade

block page
	.row
		section.col-md-9
			if articles.length > 0
				each article in articles
					+article(article)
			else
				h1.no-matches No articles to display

		section.col-md-3
			.well
				+about_me
				hr
				+get_in_touch